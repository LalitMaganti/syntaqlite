[package]
name = "perfetto-stdlib-graph"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
syntaqlite = { path = "../../syntaqlite", features = ["sqlite", "validation", "dynload"] }
